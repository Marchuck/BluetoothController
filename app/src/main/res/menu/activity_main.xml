<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/disconnect"
        android:title="disconnect"
        android:visible="false"></item>
    <item
        android:id="@+id/connect"
        android:title="connect"></item>

</menu>